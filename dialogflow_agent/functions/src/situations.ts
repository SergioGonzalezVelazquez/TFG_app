const admin = require('firebase-admin');
const functions = require('firebase-functions');

export async function handleIdentifyingSituations(agent, contexts: string[]) {
    const db = admin.firestore();
    const userId: String = 'MyGHXv1mnSTSVL1SSDlojRztW143';


}
